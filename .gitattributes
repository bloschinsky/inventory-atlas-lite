# Deployment scripts must keep LF endings so they run inside the Linux container.
*.sh text eol=lf
*.service text eol=lf

# IDE settings do not belong in the release archive built with git archive.
.idea/ export-ignore
